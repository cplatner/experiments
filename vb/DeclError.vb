
Module Module1
 
    Sub Main()
        Dim str As String = "Hello"
        Dim intLength As Integer
        intLength = Len(str)
        Console.WriteLine(str)
        Console.WriteLine(intLength)
    End Sub
 
End Module

